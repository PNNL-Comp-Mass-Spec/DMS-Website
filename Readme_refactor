-----------------------------------------------
-- DMS website migration
-----------------------------------------------

minimize or eliminate JS generated by PHP

get datepicker working

override built-in alert with JQ UI dialog

set up JQ global AJAX error call

Package DMS JS into library modules
Get rid of aux info
Reduce/eliminate in-line JS

------------------------------------------
    $(document).ajaxError(function (e, xhr, settings, exception) {
        alert('AJAX error in: ' + settings.url + '; ' + 'error:' + exception);
    });
------------------------------------------


body#Isolation_Edit #NitTableContainer .r0 {
    background: #FFF !important;
    border-left-color: #FFF !important;
    border-bottom-color: #FFF !important;
    padding-left: 0 !important;
}

<body id="bodyId">

------------------------------------------
standard interface for page family pages 

------------------------------------------
^function\s+(.*)\(  ==> $1: function(
------------------------------------------
For all base pages:
globalAJAX.site_url = '<?= site_url() ?>';

get rid of <?= site_url() ?> in cmd page JS


-----------------------
http://andykdocs.de/andykdocs/document/Migrating-from-the-Prototype-JavaScript-Framework-to-jQuery
http://webcache.googleusercontent.com/search?q=cache:http://andykdocs.de/andykdocs/document/Migrating-from-the-Prototype-JavaScript-Framework-to-jQuery

\$\(['"]([a-z0-9_-]+)['"]\) --> \$\('#$1'\)

$( --> $(

\$F\(['"]([a-z0-9_-]+)['"]\) --> \$F('#$1')
\$F\(['"]([#a-z0-9_-]+)['"]\) --> \$('$1').val()


-------------------------------------------
$('#' + globalAJAX.response_container_name) -> VF(globalAJAX.response_container_name)

\$\('#'.*\+.s*(.*)\)  -> VF($1)
-------------------------------------------
	p = $('#' + form).serialize();


	$.post(url, p, function (data) {
		    $('#' + container).html(data);
		}
	);

	$.post(url, p, function (data) {
		    $('#' + container).html(data);
			if(follow_on_action && follow_on_action.run) {
				follow_on_action.run();
			}
		}
	);


	$.post(url, p, function (data) {
		    $('#' + container_name).html(data);
		}
	);

		$.post(url, p, function (data) {
			var rows = data.evalJSON();
			draw_graph(rows);
		}
	);
	